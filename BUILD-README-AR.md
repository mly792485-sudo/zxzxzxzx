# تسليم مشروع نور الإسلام

تم إصلاح نسخة المشروع وتهيئتها للمشروع الأصلي في Capacitor. النسخة الافتراضية للبحرين تستخدم **التقويم البحريني الرسمي / منهجية روزنامة الزبارة والبحرين**، مع حساب الأيام السبعة القادمة قبل جدولة الإشعارات. تم توحيد تشغيل الأذان على ملف محلي مرفق (`adhan.wav`) يعمل دون اتصال داخل معاينة التطبيق والإشعارات المحلية. لم تتم نسبة التسجيل إلى شيخ معيّن لعدم وجود إثبات ترخيص أو مصدر موثوق داخل الملف المرسل؛ يمكن استبدال الملف نفسه بملف مرخّص من عبد الباسط عبد الصمد إذا وفره صاحب المشروع.

تم تكبير صور وأيقونات أقسام الواجهة، وإعادة توليد أيقونة iOS بقياس 1024×1024 من شعار التطبيق الموجود نفسه، وإعادة توليد أحجام Android القياسية. كما أضيفت إعدادات صلاحية الموقع، وإعدادات شاشة الإطلاق، وتحسينات iOS لمنطقة النتوء، وأصبح اختبار الإشعار يستخدم واجهة Native على الهاتف بدلاً من Notification API وحدها. نجح فحص TypeScript والبناء الإنتاجي ومزامنة Capacitor للمنصتين.

## تشغيل المشروع

```bash
npm install
npm run lint
npm run build
npx cap sync ios
npx cap sync android
```

## بناء نسخة iPhone

يلزم macOS مع Xcode 15 أو أحدث وCocoaPods. افتح مجلد `ios/App` في Xcode، ثم اختر Team وشهادة التوقيع والجهاز واضغط Run لبناء نسخة تجريبية. لإنشاء IPA قابل للتثبيت أو الرفع إلى TestFlight يلزم حساب Apple Developer وملف provisioning profile وشهادة توزيع متوافقة مع Bundle ID التالي:

```text
com.noor.alislam
```

يوجد سير عمل GitHub Actions في `.github/workflows/build-ios-signed.yml` لإصدار IPA موقّع على macOS، لكنه يحتاج أسرار Apple الخاصة بصاحب الحساب. لا يمكن إنشاء IPA أصلي موقّع داخل بيئة Ubuntu الحالية لعدم وجود Xcode وmacOS، ولذلك لا ينبغي تسمية ملف ZIP بأنه IPA.

## تشغيل المستشار الإسلامي في نسخة المتجر

المستشار ليس خدمة محلية داخل تطبيق iPhone. يجب نشر `server.ts` على خادم HTTPS، وضبط `OPENAI_API_KEY` و`APP_URL` في الخادم، ثم بناء التطبيق مع المتغير:

```bash
VITE_API_BASE_URL=https://api.example.com npm run build
```

لا تضع `OPENAI_API_KEY` في التطبيق أو في أي متغير يبدأ بـ `VITE_`؛ فكل ما يدخل في بناء الهاتف يمكن استخراجه من التطبيق. إذا لم يُضبط `VITE_API_BASE_URL` فسيعرض المستشار رسالة تعذر الاتصال بدل الفشل الصامت.

يجب نشر صفحة سياسة الخصوصية على رابط HTTPS عام، ومراجعة قسم App Privacy في App Store Connect بما يطابق استخدام المستشار ومزود الذكاء الاصطناعي.

## بناء نسخة Android

```bash
npm install
npm run build
npx cap sync android
cd android
./gradlew assembleDebug
```

ملف الأذان المرفق قصير ومناسب للإشعارات المحلية. تشغيل الأذان الطويل أو الإشعارات بعد إغلاق التطبيق يعتمد على سماح النظام بالإشعارات، إعدادات توفير البطارية، وإذن المستخدم؛ لا يمكن لأي تطبيق تجاوز هذه القيود من داخل الكود.

## المصادر

مرجع مواقيت البحرين: صفحة التقويم البحريني في موقع المجلس الأعلى للشؤون الإسلامية: https://www.almajles.gov.bh/Programs/calendar/

تقرير الفحص الداخلي موجود في `qa-findings.md`.
